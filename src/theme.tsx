import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#CE102C',
    },
    secondary: {
      main: '#3BA0DF',
    },
  },
});

export default theme;