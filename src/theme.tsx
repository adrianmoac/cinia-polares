import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#BC0D0D',
    },
    secondary: {
      main: '#3BA0DF',
    },
  },
});

export default theme;